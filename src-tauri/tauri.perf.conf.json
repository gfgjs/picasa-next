{
  "$schema": "https://schema.tauri.app/config/2",
  "//": "Perf 变体配置覆盖（§1.4.4）。经 `--features perf` 编入原生后端 glue（pdfium load-dynamic、FFmpeg sidecar）。当这些组件落地（P4/P5）后，在此追加 bundle.externalBin（ffmpeg.exe）与 bundle.resources（pdfium.dll）以产出『完整包』；不追加则为『轻量+组件包』（组件离线补装）。当前与默认配置一致。"
}
